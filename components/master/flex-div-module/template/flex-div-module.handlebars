{{#with content}}
	<div class="row">
		<div class="small-12 medium-10 medium-centered columns">
			<div class="master">
				{{#each this.cellContent}}
					<div class="grid">
						{{{this}}}
					</div>
				{{/each}}
			</div>
		</div>
	</div>
{{/with}}